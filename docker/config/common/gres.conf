# gres.conf — control-plane-only cluster, no real compute nodes.
# GresTypes=gpu is set in slurm.conf so the accounting layer (slurmdbd) tracks
# gres/gpu as a TRES, enabling QOS limits on GPU resources.
# This file intentionally defines no NodeName entries (no actual slurmd nodes).
AutoDetect=off
