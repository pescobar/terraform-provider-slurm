# cgroup.conf — shared across all Slurm versions
# Constraints disabled; IgnoreSystemd required for Docker environments without systemd.

ConstrainCores=no
ConstrainDevices=no
ConstrainRAMSpace=no
ConstrainSwapSpace=no
IgnoreSystemd=yes
IgnoreSystemdOnFailure=yes
