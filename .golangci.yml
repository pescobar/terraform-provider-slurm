# Minimal, low-noise lint config. The "standard" default enables errcheck,
# govet, ineffassign, staticcheck and unused — enough to catch dead code and
# swallowed errors without style-war churn.
version: "2"

linters:
  default: standard
  enable:
    - misspell
    - unconvert
